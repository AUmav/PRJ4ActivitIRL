import CheckToken from "../../CheckToken";
import AlterActivityForm from "../02-organisms/forms/AlterActivityForm/AlterActivityForm";

const AlterActivityPage = () => {
    return (
        <div>
            <CheckToken/>
            <AlterActivityForm/>
        </div>
    )
}

export default AlterActivityPage;